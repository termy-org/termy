[package]
name = "termy_ffi"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[lints]
workspace = true

[dependencies]
termy_core = { path = "../core" }
termy_config_core = { path = "../config_core" }
serde_json = "1.0"
