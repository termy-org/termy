# termy_wasm

WebAssembly terminal surface for browser consumers.

This crate exposes the WASM runtime consumed by `packages/libtermy-js`. Keep it aligned with reusable core/domain behavior and avoid dependencies on the desktop app or GPUI-specific terminal UI.
